apiVersion: v1
kind: Service
metadata:
  name: smart-match-backend
  namespace: smart-match
spec:
  selector:
    app: smart-match-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort
